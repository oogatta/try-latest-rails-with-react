<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= react_component("HelloWorld", { greeting: "Hello from react-rails." }) %>

<% if false %>
<%= react_component("Shibuya", { message: "wow" }) %>
<% end %>

<%= link_to 'New Post', new_post_path %>

<h3>user</h3>
<p><%= current_user %></p>
<p>user_signed_in?: <%= user_signed_in? %></p>

<h3>admin</h3>
<p><%= current_admin %></p>
<p>admin_signed_in?: <%= admin_signed_in? %></p>
